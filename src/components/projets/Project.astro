---
import ProjectsSkills from './ProjectsSkills.astro'
import Git from '../buttons/Git.astro'
import Demo from '../buttons/Demo.astro'
const { project } = Astro.props
---

<div
  class='bg-grayy rounded-2xl w-fit duration-300 h-fit hover:duration-300 hover:-translate-y-4 shadow-md shadow-gray-800'
>
  <img class='rounded-t-2xl' src={project.image} alt={project.title} />
  <div class='p-6 flex flex-col justify-around'>
    <h1 class='text-3xl font-bold pb-2'>{project.title}</h1>

    <p>{project.description}</p>

    <ProjectsSkills project={project} />

    <div class='flex items-center flex-row gap-3'>
      {project.github && <Git project={project} />}

      {project.demo && <Demo project={project} />}
    </div>
  </div>
</div>
